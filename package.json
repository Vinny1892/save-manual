{
  "name": "save-sync",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "Monorepo do save-sync: server + web UI, client de PC (Tauri) e client Android.",
  "workspaces": [
    "apps/web",
    "apps/client-pc"
  ],
  "scripts": {
    "dev": "npm run dev -w @save-sync/web",
    "build": "npm run build -w @save-sync/web",
    "check": "npm run check -w @save-sync/web",
    "tauri": "npm run tauri -w @save-sync/client-pc --"
  }
}
